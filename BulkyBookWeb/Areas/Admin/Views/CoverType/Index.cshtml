@model IEnumerable<BulkyBook.Models.CoverType>

@{
    ViewData["Title"] = "Index";
}





<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Cover-Type List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create New Cover-Type
            </a>
        </div>
    </div>
</div>





<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>Cover-Type ID</th>
            <th>Cover-Type Name</th>            
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (CoverType cover in Model)
        {
            <tr>
                <td>@cover.Id</td>
                <td>@cover.Name</td>                
                <td class="text-center" style="width:25%">
                    <div class="w-75 btn-group" role="group">
                        <a class="btn btn-primary mx-2" asp-controller="CoverType" asp-action="Edit" asp-route-id="@cover.Id">
                            <i class="bi bi-pencil-square"></i> &nbsp; Edit
                        </a>
                        <a class="btn btn-danger mx-2" asp-controller="CoverType" asp-action="Delete" asp-route-id="@cover.Id">
                            <i class="bi bi-trash-fill"></i> &nbsp; Delete
                        </a>
                    </div>
                </td>
            </tr>
        }
        
    </tbody>
</table>
